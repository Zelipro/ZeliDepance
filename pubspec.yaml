name: zelidepance
description: ZeliDepance - Application de saisi des mes depances
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flet: any

flutter:
  uses-material-design: true
